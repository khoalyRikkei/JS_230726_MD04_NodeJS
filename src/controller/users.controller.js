import { query } from "express";

class UserController {
  // Tạo điều hướng và phản hồi
  getDataUser(req, res) {
  }

  editDataUser(req, res, id) {}

  editPasswordUser(req, res, id) {}
}

export default UserController;
